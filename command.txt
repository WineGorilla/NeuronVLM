# 生成训练数据
python scripts/build_data.py \
    --anno data/annotations/llava_instruct_150k.json \
    --imgs data/images/train2014 \
    --max 1000

# 训练SAE
python -m src.train

# 构建feature缓存 + 标注
python scripts/interpret.py --layer 8
python scripts/interpret_local.py --layer 24
python scripts/interpret.py --layer 8 --debug_feature 38 查看某个feature的图片效果

# 查看标注
python scripts/explore.py --layer 8 --show_labels

# 可视化某个feature
python scripts/explore.py --layer 8 --feature 100
python scripts/explore.py --layer 24 --feature 6
# 单张图热图
python scripts/visualize.py --image data/images/train2014/COCO_train2014_000000000009.jpg --layer 8


流程顺序
# 训练SAE
python -m src.train
# 缓存每张图片的神经元激活
python scripts/build_cache.py --layer 8
# 转换为Index方便计算筛选
python scripts/build_feature_index.py --layer 8
# 进行特曾层SAE的神经元标注
python scripts/interpret.py --layer 8
# 为数据集添加每个问题对应调用的神经元
python scripts/build_feature_data.py






#src folder
Model.py: 整体模型结构
SAE: SAE结构
train.py: 训练SAE
train_router.py: 训练整体模型
datasets.py: 构建训练数据

#scripts folder
build_data.py: 构建初始数据集，包含图片，问题，回答
build_data_feature: 构建模型训练数据集，除了图片，问题，回答，还有思考所用的神经元
build_feature_index: 讲每张图片的神经元激活转换为更方便计算的方式，得到feature_index_layerx






# 1. 训练 SAE
python -m src.train

# 2. 构建缓存和索引
python scripts/build_cache.py --layer 8
python scripts/build_feature_index.py --layer 8

# 3. 标注 feature
python scripts/interpret.py --layer 8

# 4. 聚类
python scripts/cluster_features.py --layer 8 --n_clusters 32

# 5. 构建训练数据
python scripts/build_cluster_data.py --layer 8

# 
# 阶段1：训练 ClusterPredictor（BCE loss）
python -m src.train_focus --stage 1

# 阶段2：冻结 ClusterPredictor，微调 Qwen 最后8层（LM loss）
python -m src.train_focus --stage 2 --resume best

# 8. 推理
python scripts/inference.py \
    --image data/images/train2014/xxx.jpg \
    --question "What color is the dog?"




# 模型训练

# train_focus
阶段1（默认）：python -m src.train_focus
    只训练最后8层 + lm_head
    数据：train_cluster.jsonl 里的问题 → <think>[ids]</think>
    让模型学会根据问题输出正确的 cluster

阶段2（联合）：python -m src.train_focus --joint
    冻结predictor,在阶段1基础上继续训练


# Inference推理过程
# 只用阶段1（只有 predictor）
python scripts/inference.py \
    --image xxx.jpg \
    --question "What color is the dog?" \
    --predictor_ckpt outputs/focus_ckpt/predictor_best.pt

# 阶段1 + 阶段2（predictor + 微调 Qwen）
python scripts/inference.py \
    --image xxx.jpg \
    --question "What color is the dog?" \
    --predictor_ckpt outputs/focus_ckpt/predictor_best.pt \
    --qwen_ckpt      outputs/focus_ckpt/qwen_best.pt


# 调参专用
python scripts/build_cluster_data.py --layer 8 --max 5000 && python -m src.train_focus --stage 1 && python -m src.train_focus --stage 2 --resume best && python eval/eval_cvbench.py --mode enhanced
python eval/eval_cvbench.py --mode enhanced --qwen_ckpt outputs/focus_ckpt/qwen_best.pt


CUDA_VISIBLE_DEVICES=1 python eval/eval_cvbench_copy.py --mode enhanced --qwen_ckpt outputs/focus_ckpt/qwen_best.pt


CUDA_VISIBLE_DEVICES=1 python -m src.train_focus_copy --stage 1 && python -m src.train_focus_copy --stage 2 --resume best && python eval/eval_cvbench_copy.py --mode enhanced

python -m ablation.train_no_pcs --stage 1 && python -m ablation.train_no_pcs --stage 2 --resume best && python eval/eval_cvbench.py --mode no_pcs --no_pcs_qwen_ckpt outputs/ablation_no_pcs/qwen_best.pt

CUDA_VISIBLE_DEVICES=1  python eval/eval_cvbench.py --mode no_pcs --no_pcs_qwen_ckpt outputs/ablation_no_pcs/qwen_best.pt

CUDA_VISIBLE_DEVICES=1 python eval/eval_mmstar.py --mode enhanced


CUDA_VISIBLE_DEVICES=1 python eval/eval_cvbench.py --mode enhanced --qwen_ckpt outputs/focus_ckpt/qwen_best.pt



CUDA_VISIBLE_DEVICES=0 python -m src.train && python scripts/build_cache.py --layer 8 && python scripts/build_feature_index.py --layer 8
CUDA_VISIBLE_DEVICES=1 python -m llava.train_sae && python llava/build_cache_llava.py --layer 8